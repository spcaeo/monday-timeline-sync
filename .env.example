# Monday.com OAuth App Credentials
# Get these from https://monday.com/developers/apps
MONDAY_CLIENT_ID=your_client_id_here
MONDAY_CLIENT_SECRET=your_client_secret_here

# Monday.com Webhook Signing Secret
# Used to verify incoming webhook requests from monday.com
MONDAY_SIGNING_SECRET=your_signing_secret_here

# Monday.com API Token (optional, for non-OAuth usage)
# Get from https://monday.com/developers/apps > API Token
MONDAY_API_TOKEN=your_api_token_here

# Public URL where this app is hosted
# Used for OAuth redirect and webhook callbacks
APP_URL=https://your-app-url.example.com

# Server port (default: 3000)
PORT=3000

# Sync mode: "bidirectional" or "one-way"
# bidirectional: changes on either board sync to the other
# one-way: changes only sync from source to target
SYNC_MODE=bidirectional
